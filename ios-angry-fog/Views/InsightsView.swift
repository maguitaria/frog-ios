import SwiftUI

struct InsightsView: View {
    var body: some View {
        VStack {
            Text("📊 Safety Trends")
                .font(.title)
                .padding()
            Text("No significant alerts in your region over the past week.")
        }
    }
}